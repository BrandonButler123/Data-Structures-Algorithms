package com.keyin;

//TIP To <b>Run</b> code, press <shortcut actionId="Run"/> or
// click the <icon src="AllIcons.Actions.Execute"/> icon in the gutter.
public class Main {
    public static void main(String[] args) {
            QueueArray queueArray = new QueueArray(5);
            queueArray.isEmpty();
        }




        // An animal shelter, which holds only dogs and cats, operates on a strictly FIFO basis.
        // People must adopt either the "oldest" (based on arrival time) of all animals at the shelter,
        // or they can select weather they would prefer a dog or cat (and will receive the oldest animal
        // of that type). They cannot select which specific animal they would like. Create the data
        // structure to maintain this system and implement operations to enQueue, deQueueAny, deQueueCat, deQueueDog
    }